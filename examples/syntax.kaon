import something

trait Sound {
    say(noise: String) -> String {
        print(noise) 
    }
}

class Dog {
    var name

    constructor new(name: String) {
        name = name
    }
}

use Sound with Dog

var dog = Dog.new("Scott")
dog.say("bark")