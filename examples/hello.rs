use kaon::Kaon;

fn main() {
    /*let mut kaon = Kaon::new();

    let scope = kaon.globals();

    scope.add::<f32>("x", 40.0);
    scope.add::<&str>("y", "hello");
    scope.add::<bool>("z", true);*/
}