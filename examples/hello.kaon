var global = "global"

{
    var global = "inner"

    {
        var global = "innermost"
        global = "inner-innermost"
        {
            println(global)
        }
    }

    println(global)
}

println(global)