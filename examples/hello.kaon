class Point {
    var x = 0
    var y = 0

    const new(a, b) {
        this.x = a
    }

    fun add(other: Point): Point {
        return Point.new(this.x + other.x, this.y + other.y)
    }
}

var instance = Point.new(1, 2)

io.println(instance)