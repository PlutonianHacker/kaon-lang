class Point {
    var x = 0
    var y = 0

    create new(a, b) {
        io.println(self, a, b)
    }

    fun add(other: Point): Point {
        return Point.new(self.x + other.x, self.y + other.y)
    }
}

var instance = Point.new(1, 2)
var other = Point.new(3, 4)
