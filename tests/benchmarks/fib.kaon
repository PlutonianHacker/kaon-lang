fun fib(n): f64 {
    if n < 2 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

var start = os.clock()
io.println(fib(35)) // 317,811
io.println("elapsed: ", io.to_string(os.clock() - start))
